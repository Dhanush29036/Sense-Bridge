global:
  scrape_interval:     15s
  evaluation_interval: 15s

scrape_configs:
  # Scrape the SenseBridge Node.js backend
  - job_name: 'sensebridge-backend'
    static_configs:
      - targets: ['host.docker.internal:5000']
    metrics_path: '/metrics'
    scrape_interval: 10s
